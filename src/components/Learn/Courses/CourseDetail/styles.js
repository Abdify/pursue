import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
    courseInfo: {
        margin: "1rem"
    },
    chapterBtn: {
        display: "flex",
        justifyContent: "flex-start"
    }
});
